import type { Options } from '../types/markdown';
import type { MuiMarkdownProps } from '../types/muiMarkdown';
declare const useOptions: (props: MuiMarkdownProps) => Options;
export default useOptions;
